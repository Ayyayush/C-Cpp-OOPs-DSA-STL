#include <iostream>
using namespace std;

int main()
{

  char str[30], ch = ' ';
  int i, k, count = 0;

  cout << "Enter the string:" << endl;
  gets(str);
  // Initializing for loop.
  for (i = 0; str[i] != '\0'; ++i)
  {
    count++;
  }

  cout << "Length of the string is:" << length << endl;
  for (i = 0; str[i]; i++)

    if (str[i] == ch)
      c++;
  cout << "The number of spaces is " << c << endl;
  return 0;
}
